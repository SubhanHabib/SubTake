// Keep Slint's scrolling/keyboard/accessibility implementation behind one app boundary.
// Native scrollbar internals remain provided by the selected Slint widget style.
import { ScrollView } from "std-widgets.slint";
export component ScrollArea inherits ScrollView { }

# Native Mac validation record

> GPUI migration notice: UI descriptions and validation evidence below predate the migration and are retained as historical reference. They do not certify current GPUI behavior. See [GPUI-MIGRATION.md](GPUI-MIGRATION.md) for current example coverage and pending acceptance gates.

Recorded 12 September 2026 against parent revision `7512ef1518657edc2caae595136821d27a47167d`. This records the local build and checks actually run; it is not a full Mac/Windows parity certification. Machine-readable measurements, source hashes and the packaged executable hash are in [validation.json](validation.json).

## Host and package

- Apple M5 Pro, 48 GiB RAM, Apple Silicon.
- macOS 27.0, build 26A5421a. macOS 14 and current stable macOS were not tested on this host.
- Optimized Rust build, native GPUI interface, Metal compositor, in-process FFmpeg software decoding.
- Local bundle disk usage after the UI refresh: **119M**, excluding the separately downloaded Whisper model and user projects.
- Ad-hoc signing: `codesign --verify --deep --strict` passed. Developer ID signing/notarization is not completed.
- All 28 bundled binaries/libraries were inspected with `otool -L`: no external Homebrew dependency paths remain.
- Packaged media/UI checks ran with `PATH=/usr/bin:/bin` and bundled runtimes. No Electron or WebView is used by the new app.

## Automated and device checks

**44 default tests passed, zero failed.** One test requiring a real output device is excluded from the default suite; it was explicitly run on this Mac and also passed, making **45 executed Rust tests** across the two invocations.

- Seven document tests: versioned documents, unknown-field preservation, transactional history and save behavior.
- Five media integration tests: source-discovery process cancellation, decoder frame caching, export cancellation preserving an existing destination, rotation/EOF frame behavior, and preview edit bounds enclosing the actual rendered annotation.
- One preset-removal unit test verifies retained bytes and rejects files outside the managed directory.
- Twenty regression tests: crop bounds including malformed imports, folder search/deduplication, timeline/editing semantics, grouped movement at source edges, caption word retiming, subtitles, paths, presets, language catalogs, shortcuts, deterministic motion, capture-file replacement, recovery and bounded/cancellable processes.
- Eleven reference tests, including a corpus generated by importing the existing product's TypeScript: **24 geometry cases; five spring configurations × 90 steps; 106 zoom strengths; 288 caption layout/animation cases; 192 export dimensions; five Whisper token reconstructions; ten auto-zoom cases; 270 phrase segmentation cases; 12 sentence-ending cases; 12 split/merge inputs checked for both operations.**
- Real default audio output: played **silence only**, verified the audio clock progressed for the source duration, and verified cancellation returned promptly without jumping the clock to the end. Playback now reports a failed FFmpeg producer instead of treating it as successful completion.

Commands:

```sh
cargo test --locked
cargo test --locked --test media_integration silent_output_device_clock_and_cancel -- --ignored
node scripts/reference-fixtures.mjs
```

## Packaged media and UI

The generated scene includes a 640 × 360 source, a video background, webcam, SVG cursor assets, camera/cursor motion, text/blur annotations, captions, trim and speed changes, system/mic sidecars and added music.

- Software H.264: **90 frames at 30 fps, 3.000 seconds of video and 3.000 seconds of audio**.
- VideoToolbox H.264: the same frame count and video/audio durations passed.
- GIF: **30 frames at 10 fps**.
- SRT/VTT sidecar timings passed the trim/speed mapping check.
- Spectral audio checks verified the intended system/microphone/music gains and checked that embedded audio was not doubled with its system companion.
- UI smoke passed at **1360 × 880** and **980 × 680**, at the minimum size in French, and in the visual Crop panel. Checks include load, add/undo/redo, copy/cut/paste, clip split, caption split/merge/word editing, group selection/deletion, visual crop/undo, folder library search and seeking. The selected inspector must match the displayed selector index.
- UI smoke settings/recovery are isolated in a per-process temporary directory. A before/after byte comparison verified that the packaged UI smoke runs did not change user preferences.
- English, French and crop screenshots were visually inspected. The later Recordly UI refresh also covers Scene, Background, Export, Presets and Captions; `ui-validation.json` records eleven packaged runs, including Settings and empty recording at both sizes. Unmatched native labels fall back to English; full translation coverage is not claimed.

The official Whisper Small model was downloaded and used locally on generated speech from a microphone sidecar with no embedded video audio. The recognized transcript was:

> Welcome to Subtake. This native editor records your screen, adds captions, and exports your finished video.

Silence/sentence postprocessing split this into two phrases. This run did not produce usable word timing, so the existing fallback semantics apply; it does not validate precise multilingual word alignment.

## Performance observations

These performance and idle samples predate the Recordly UI refresh. They are retained as historical measurements and do not establish the redesigned UI’s idle, startup or power consumption. Each CLI benchmark rendered 90 frames and then ten deterministic random seeks. Times include decoding, native composition and RGBA readback. They exclude UI presentation, audio and whole-app startup. First-frame time begins at compositor construction, after probing the media. Results are short local measurements, not sustained/power benchmarks.

| Workload | First composed frame | Sequential throughput | Seek median | Seek p95 |
|---|---:|---:|---:|---:|
| Composed 640 × 360 source → 960 × 540 | 138.6 ms | 288.8 fps | 16.2 ms | 25.7 ms |
| Same source/scene → 1920 × 1080 | 147.0 ms | 261.7 fps | 13.5 ms | 29.5 ms |
| 3840 × 2160 source → 960 × 540 | 81.7 ms | 154.8 fps | 251.2 ms | 368.3 ms |

The 4K fixture is a five-second H.264 test pattern encoded with a long interval between keyframes. Its webcam/video background are disabled and it has no cursor sidecar, so it is a different workload, not an isolated output-resolution comparison. Its roughly quarter-second random seek shows why the small-fixture result must not be generalized to every recording.

The earlier native subprocess decoder measured 486.6 ms median seek on the small 960 × 540 scene. The current in-process decoder avoids restarting FFmpeg for each seek. This is an internal native-build comparison; **no matched Electron comparison or battery claim is made**.

With the generated project open and paused, ten process-family samples reported a mean CPU value of **0.0%** and a maximum resident set of **144.0 MiB**. CPU values are rounded `ps` snapshots; resident memory excludes driver/GPU allocations. This short idle observation is not a power measurement or a long-session memory test.

## Remaining limits

- **Real microphone/camera/system-audio integration, long-session pause/resume drift and permission revocation remain unverified.** The short screen-only helper capture described below passed after permission was granted.
- Audible/perceptual A/V synchronization, output-device changes, long sessions, VFR/HDR/long-GOP coverage, device removal, sleep/wake and mixed-DPI capture remain to be exercised.
- Finder document events, fullscreen/Spaces HUD behavior and shortcut conflicts are implemented but not fully exercised on the packaged app.
- Full frame-by-frame visual equivalence, all interaction details, complete translation coverage and manual acceptance of the new crop/library/preset controls are still listed in [PARITY.md](PARITY.md).
- No Windows/Linux build, Intel Mac/Universal package, notarization, installer or updater was validated.

Detailed generated media, images and raw logs are in ignored `test-output/`. The current app is left open at the recording overlay for review. Windows-specific implementation and acceptance work is in [WINDOWS-HANDOFF.md](WINDOWS-HANDOFF.md).

## Recording/UI glitch follow-up

Screen permission was verified as granted. A real capture of the empty SubTake window using the packaged ScreenCaptureKit helper passed start, pause, resume and stop, saving a 2720 × 1824 MP4 lasting 3.026667 seconds. Camera, microphone and system audio were disabled; no audio track was produced. This historical check exercised helper controls, but did not establish selected-window isolation or multi-device synchronization. The recorder now saves automatically, and its current acceptance is below. See [RECORDING.md](RECORDING.md) for the user flow and exact scope.

The source enumeration busy-state bug is fixed. UI regression checks explicitly cover success, empty results, failure, cancellation, retained source selection and reopening configuration without starting capture. Eleven packaged snapshots include the empty recording screen and Settings. The 44 default Rust tests include prompt subprocess cancellation. Historical media, performance and audio-output checks above retain their original scope.

## Menu-bar recorder acceptance

The recorder entry point is a separate borderless GPUI window. Normal launch uses the Mac accessory policy, with an icon-only Open/Quit menu. The editor stays hidden through setup and recording; completed media opens the editor, and the recorder hides. The same overlay handles countdown, cancellation, elapsed time, pause/resume and stop. Recordings save automatically to Movies/SubTake or a chosen folder.

`launcher-validation.json` records six setup snapshots plus a complete recording lifecycle test restricted to the generated SubTake Capture Fixture window. It checks source-button pointer events, hidden/reopened overlay lifetime, cancelled countdown, actual capture, pause/resume acknowledgements, stop/finalization and the recorded project opening in the editor. The camera, microphone and system audio were off. User preferences were unchanged. The captured video, generated editor snapshot and detailed media probe are under `test-output/launcher/`.

This complete lifecycle test supersedes the earlier helper-only acceptance limit for screen recording. Fullscreen/Spaces, real device combinations, long recordings, permission changes and Windows integration still need acceptance. The pre-existing user instance with an unsaved recording was preserved while testing separate processes.

The native capture helper now uses an explicit selected-window inclusion filter for window recording, retaining its crop and window tracking. The earlier display-crop implementation could include another application covering the chosen window. The current capture test covers the blue fixture with a separate red window and checks four saved frames (0.25, 0.75, 1.5 and 2.5 seconds): the selected blue content is present and the red occluder is absent. Both lifecycle and image-content checks passed on the packaged helper. Initial retries while both displays were asleep produced no frames; the successful run kept the displays awake. This does not establish sleep/wake recovery or mixed-display movement.

## GPUI real-app walkthrough

On 23 September 2026, the GPUI build passed `SUBTAKE_WALKTHROUGH` (see [RUNNING.md](../RUNNING.md)) in both appearances. The run went through launcher cards, countdown, a capture of the laptop's built-in display with pause and resume, stop and finalisation, the editor with playback, every inspector panel, visual crop, a zoom region, Presets and an export. Both exports probed at **3240 × 1820** and ran 9.4 s. The camera and microphone were off, and settings were isolated.

The runs were filmed on the built-in display with the desktop behind the app, and other apps' windows were hidden. Input came from the controls' callbacks; pointer hit testing was not exercised.

A first attempt had matched sources by a "Built-in" name. Sources are named "Display N", so it recorded an external display instead, and that explained a 5414 × 3044 export. The walkthrough now asks CoreGraphics which display is built in.

One issue is still open: the clip lane shows flat mint until its thumbnails load.

## Repository root migration

The native project now occupies the repository root; the Electron application, original workflows and shell spike are preserved in `legacy-electron/`. Build helpers and reference generators use that explicit location. The package is `dist/SubTake.app`. Root Rust tests and packaged editor/recorder layout checks are rerun after the move. The prior screen-capture acceptance is preserved separately in `capture-validation-before-root-move.json`; its timestamps and original paths describe that earlier run, whose generated media now lives under root `test-output/`. Moving the repository does not establish any additional device or platform acceptance.
